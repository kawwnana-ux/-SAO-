# Streamlit Community Cloud用 requirements.txt
# このファイルは app.py・translate_sao.py・patent_pipeline.py・
# en_relation_rules.py と同じリポジトリにコミットしてください。

streamlit>=1.32

# GiNZA（日本語依存構造解析）関連
# ja-ginzaが依存パッケージ（spacy, sudachipy, sudachidict_core等）を
# 連鎖的にインストールするため、他は明示不要。
spacy>=3.4,<3.8
ginza>=5.1
ja-ginza>=5.1

# グラフ構造（patent_pipeline.py内の階層関係の扱いで使用）
networkx

# グラフ描画（patent_pipeline.py内で使用。公開デモではapp.py側の
# st.graphviz_chartを主に使うが、patent_pipeline.py全体のimportが
# 通るように残す）
matplotlib

# ローカルOllama用クライアント（patent_pipeline.py冒頭でimportされる
# ため、Streamlit Cloud上でもインストールが必要。公開デモでは
# translate_sao._ollama_chatをOpenRouter経由の関数に差し替えるため、
# 実際にOllamaサーバーへ接続することはない）
ollama

# 公開デモ用クラウドLLMバックエンド（OpenRouter経由でqwen2.5-7b-instruct
# を呼び出すために使用。OpenAI SDKはOpenRouterとAPI互換）
openai>=1.0

# 表データ処理（統計分析ページ・精度検証ページのCSV/Excel読み込み、
# 検索ページのデータベース管理などで使用）
pandas

# 精度検証ページでの.xlsxアップロード読み込み（pandas.read_excelのバックエンド）
openpyxl

# 「2つの請求項を比較」ページの意味マッチング（②）、「まとめて検索」ページの
# 埋め込みベースの検索（patent_pipeline.py内の_get_embed_model/
# semantic_similarityで使用）。多言語対応モデル
# （paraphrase-multilingual-mpnet-base-v2、初回読み込み時に自動ダウンロード
# ／モデル本体1GB超）を使うため、torch込みで比較的重い依存関係です。
# Streamlit Community Cloudの無料枠（メモリ制限あり）では、これらの機能を
# 使うと動作が遅くなったり、メモリ不足でアプリが再起動する場合があります。
sentence-transformers
