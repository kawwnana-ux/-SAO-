# 実験12：抽出の弱点を汎用的な仕組みでまとめて改善する

## 1. 目的

実験11（主指標 F1 51.34）の誤りを全件分類すると、次の3種類の弱点が残っていた。

1. 余計な抽出（FP 3,219件）：組み合わせる相手の取り違え、中間を飛ばした近道、ノードの粒度の誤り
2. 取り逃し（FN 5,760件）：どの抽出方法も出していない組（1,244件）、ノードはあるのに関係が選ばれない
3. 苦手な請求項と関係：方法の請求項（順次列挙形式、F1 22.9）、「有する」系以外の関係（再現率 34.9%）

実験12では、特定の書き方ごとに規則を足すのではなく、どの請求項にも同じように働く仕組みでこれらをまとめて改善することを目指した。評価の条件は実験11と同じにした。主指標はトリプル完全一致で、532件を5分割の入れ子交差検証で評価し、評価する請求項は学習に使っていない。学習ラベルとモデル選択にも完全一致を使った。

## 2. 試した仕組み

| 仕組み | ねらい | 結果 |
|---|---|---|
| (1) 係り受けに基づく候補生成（dep_pairs.py） | 述語にかかる構成要素どうしをすべて候補の組にし、格助詞・受身・連体修飾・並列を特徴量として与える（節ベースの Open IE と同じ考え方）。取り逃し（2）と、「有する」系以外の関係（3）への対策 | **採用** |
| (2) 2段階の選別 | 1段目の確率から「同じ組の中での順位」「逆向きの組」「同じ部品を持つ別の持ち主」「A→B→Cの近道」「各ノードで最も確からしい関係か」を作り、2段目で選び直す。余計な抽出（1）への対策 | **採用** |
| (3) 1組から2件目の関係も採る | 同じ2つのノードの間に複数の関係がある正解（196件）への対策 | 不採用 |
| (4) 持ち主つきノード「XのY」の候補 | ノードが短すぎる誤り（1）への対策 | 不採用 |
| (5) 持ち主の情報で「XのY」と「Y」を同じ組とみなす | 同上 | 不採用 |

(3)では、同じ組から採る関係の最大数（1か2）をしきい値と一緒に内側の交差検証で選ばせた。その結果、5つの分割すべてで「1」が選ばれた。(4)は候補が75,464件に増えたのに正解は340件（0.4%）しかなく、選別モデルが選んだ6件もすべて誤りだったため外した。

しきい値などを選ぶ内側の評価は、主指標と同じく「1つの正解には1つの抽出だけを対応させる」数え方に直した。以前の数え方では、同じ正解に当たる同義の関係（「有する」と「備える」など）が2件とも正解に数えられ、1組から2件採る設定が有利に見えてしまっていた。

## 3. 結果（主指標：トリプル完全一致、532件）

| 条件 | 候補 | 選別 | 適合率 | 再現率 | F1 | 実験11との差 [95%信頼区間] |
|---|---|---|---|---|---|---|
| 実験11 | 実験11の候補 | 1段 | 59.54 | 45.13 | 51.34 | ― |
| A | 実験11の候補 | 1段（内側の評価を1対1に修正） | 61.09 | 45.20 | 51.96 | +0.62 [+0.23, +1.01] |
| B | ＋係り受け候補 | 1段 | 60.50 | 47.54 | 53.24 | +1.90 [+1.27, +2.53] |
| **C（採用）** | **＋係り受け候補** | **2段** | **60.48** | **48.11** | **53.59** | **+2.25 [+1.64, +2.88]** |
| D | ＋係り受け候補 | 2段＋持ち主の同一視 | 61.18 | 47.47 | 53.46 | +2.12 [+1.46, +2.77] |

信頼区間は、請求項を単位にした1,000回のブートストラップで求めた。

要素ごとの効果（ブートストラップ95%信頼区間）は次のとおり。

| 要素 | 比較 | 差 |
|---|---|---|
| 係り受け候補 | A→B | +1.28 [+0.71, +1.88] |
| 2段階の選別 | B→C | +0.35 [−0.04, +0.75] |
| 持ち主の同一視 | C→D | −0.13 [−0.42, +0.14] |

改善の大部分は係り受け候補によるもので、信頼区間は0をまたがない。2段階の選別は、主指標で最も高くなるため採用した。ただしその差は小さく、信頼区間は0をわずかにまたぐ。卒論では、「主指標を固定し、主指標が最も高い組み合わせを採用した」と書くのが正確である。持ち主の同一視は、1段の選別では予測が全く変わらず、2段では−0.13だったので採用しなかった。

候補全体に正解が含まれる割合（選別がどれだけ完璧でも超えられない再現率の上限）は、実験11の56.6%から62.7%に上がった（持ち主つきノードも加えると64.0%）。

## 4. 弱点ごとの変化（実験11 → 実験12）

| 弱点 | 実験11 | 実験12 | 変化 |
|---|---|---|---|
| 正解数（TP） | 4,737 | 5,050 | +313 |
| 余計な抽出（FP） | 3,219 | 3,300 | +81（適合率は 59.5% → 60.5%） |
| 取り逃し（FN） | 5,760 | 5,447 | −313 |
| 　うち「組がどの抽出方法からも出ていない」 | 1,244 | 831 | **−413（−33%）** |
| 　うち「正解ノードを構成要素として切り出せていない」 | 926 | 836 | −90 |
| 　うち「組は候補にあったが選別で落とした」 | 826 | 965 | +139（候補が増えたため） |
| 「有する」系以外の関係の再現率 | 34.9% | 40.0% | **+5.1pt** |
| 「有する」系の再現率 | 58.4% | 58.7% | +0.3pt（維持） |
| 構成要素列挙形式 F1（255件） | 54.7 | 57.0 | +2.3 |
| ジェプソン的形式 F1（251件） | 49.6 | 51.9 | +2.3 |
| 順次列挙形式（方法の請求項）F1（26件） | 22.9 | 23.8 | +0.9 |

余計な抽出の種類別の件数は、実験11とほぼ同じだった。組み合わせる相手の取り違え（758→780）、近道（653→634）、ノードが短い（616→658）などである。正解数が増えた分、適合率は1ポイント上がった。

## 5. 考察と限界

- **取り逃しの主な原因は、選別ではなく候補の作り方にあった。** 述語と格の手がかりだけを使う汎用の候補生成で、「組がどの抽出方法からも出ていない」取り逃しが3分の1減った。特に「接続される」「封止する」など、「有する」系以外の関係の再現率が5ポイント上がった。規則を足していないので、半導体以外の分野の請求項にもそのまま使える。
- **方法の請求項（順次列挙形式）はほとんど改善しなかった。** 26件と少なく、正解の書き方も「工程」を主語にするなど、装置の請求項と構造が異なる。候補の作り方ではなく、正解データの付け方を含めた別の扱いが必要と考えられる。今後の課題とする。
- **余計な抽出の種類はほとんど変わらなかった。** 2段階の選別で構造的な特徴を与えても、組み合わせる相手の取り違えと近道は減らなかった。これらは請求項の文面だけでは判断が難しく、人の確認が最も役に立つ部分である。この点が、アプリで「要確認」を人が確認する設計の根拠になる。
- **人と協働する使い方の根拠。** 交差検証の確率で候補を「採用」「要確認」「除外」に分けると、次のようになった。
  - 採用（4,141件）：そのうち正しいものは80.2%
  - 要確認（8,643件、1件の請求項あたり約16件）
  - 正しいSAO全体のうち31.7%が「採用」に、22.8%が「要確認」に入った
  - 「除外」に入ったのは8.3%、候補に無いのは37.3%

  採用はそのまま使い、要確認だけを人が確認すれば、自動抽出だけの場合より多くの正解を効率よく確定できる。

## 6. 再現方法（自分のPC）

```
python3 eval_translate_sao.py --pipeline-dir . --data-dir . --mode selected12 --eval-mode exact --limit 532 --llm-cache llm_cache.json --out exp12_exact.json
```

交差検証の分割ごとに、その分割を除いた請求項だけで選別モデルを学習して評価する。先頭40件で、交差検証の予測と件数が完全に一致することを確認済み。532件で F1 ≒ 53.59 になる見込みである。
