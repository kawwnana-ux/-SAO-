# 特許分析プラットフォーム（app.py）用 requirements.txt
# Streamlit Community Cloud でも、自分のPCでも同じものを使う。

# 画面（ページ切り替え st.navigation・グラフのクリック選択に 1.40 以上が必要）
streamlit>=1.40

# GiNZA（日本語の係り受け解析）
spacy>=3.4,<3.8
ginza>=5.1
ja-ginza>=5.1

# 候補選別モデル（HistGradientBoosting）・TF-IDF・類似度
scikit-learn>=1.3
numpy
scipy
pandas

# グラフ（ネットワーク・レーダー・バブル・ヒートマップ・類似性マップ）
plotly>=5.20
networkx
matplotlib

# Excel の書き出し・読み込み
openpyxl

# LLM：ローカルは Ollama、公開デモは OpenRouter（OpenAI SDK 互換）
ollama
openai>=1.0

# 「2つの請求項を比較」の意味マッチングと「まとめて検索（自分で登録）」で使う埋め込みモデル
# （初回に数百MBのダウンロード。無料枠ではメモリ不足になることがある）
sentence-transformers
